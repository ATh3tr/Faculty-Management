import { useEffect, useState } from "react";
import { Megaphone, Send } from "lucide-react";
import { useBranding } from "../branding/BrandingContext";
import { ActionForm, Card, FormField, PageHeader } from "../components/ui";
import { api, json } from "../lib/api";
import { useLanguage } from "../lib/i18n";
import type { AcademicYear, Division, Student } from "../types";

export function AnnouncementsPage() {
  const { pick } = useLanguage(); const { facultyName } = useBranding(); const [years, setYears] = useState<AcademicYear[]>([]); const [divisions, setDivisions] = useState<Division[]>([]); const [students, setStudents] = useState<Student[]>([]);
  const [form, setForm] = useState({ titleArabic: "", titleEnglish: "", bodyArabic: "", bodyEnglish: "", audience: "Everyone", studyYear: "", divisionId: "", studentUserId: "" });
  useEffect(() => { api<AcademicYear[]>("/api/catalog/academic-years").then(setYears); }, []);
  useEffect(() => { const current = years.find(x => x.isCurrent); if (current) api<Division[]>(`/api/catalog/divisions?academicYearId=${current.id}`).then(setDivisions); }, [years]);
  useEffect(() => { if (form.audience === "Student") api<Student[]>("/api/catalog/students").then(setStudents).catch(() => setStudents([])); }, [form.audience]);
  const set = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const publish = async () => { await api("/api/announcements", { method: "POST", ...json({ ...form, studyYear: form.studyYear ? Number(form.studyYear) : null, divisionId: form.divisionId || null, studentUserId: form.studentUserId || null }) }); setForm(current => ({ ...current, titleArabic: "", titleEnglish: "", bodyArabic: "", bodyEnglish: "" })); };
  return <><PageHeader eyebrow={pick("اتصال مباشر", "Direct communication")} title={pick("نشر إعلان", "Publish announcement")} description={pick("اكتب الرسالة باللغتين وحدد الجمهور المستهدف بدقة.", "Write the message in both languages and choose its exact audience.")} /><div className="two-panel"><Card title={pick("محتوى الإعلان", "Announcement content")}><ActionForm onSubmit={publish} submitLabel={pick("نشر الآن", "Publish now")}><div className="two-columns"><FormField label={pick("العنوان بالعربية", "Arabic title")}><input dir="rtl" required value={form.titleArabic} onChange={e => set("titleArabic", e.target.value)} /></FormField><FormField label={pick("العنوان بالإنكليزية", "English title")}><input dir="ltr" required value={form.titleEnglish} onChange={e => set("titleEnglish", e.target.value)} /></FormField></div><div className="two-columns"><FormField label={pick("النص بالعربية", "Arabic body")}><textarea dir="rtl" rows={6} required value={form.bodyArabic} onChange={e => set("bodyArabic", e.target.value)} /></FormField><FormField label={pick("النص بالإنكليزية", "English body")}><textarea dir="ltr" rows={6} required value={form.bodyEnglish} onChange={e => set("bodyEnglish", e.target.value)} /></FormField></div><FormField label={pick("الجمهور", "Audience")}><select value={form.audience} onChange={e => set("audience", e.target.value)}><option value="Everyone">{pick("الجميع", "Everyone")}</option><option value="StudyYear">{pick("سنة دراسية", "Study year")}</option><option value="Division">{pick("شعبة", "Division")}</option><option value="Student">{pick("طالب محدد", "Specific student")}</option><option value="Staff">{pick("الكادر", "Staff")}</option></select></FormField>{form.audience === "StudyYear" && <FormField label={pick("السنة", "Study year")}><select required value={form.studyYear} onChange={e => set("studyYear", e.target.value)}><option value="">—</option>{[1,2,3,4,5].map(x => <option key={x} value={x}>{x}</option>)}</select></FormField>}{form.audience === "Division" && <FormField label={pick("الشعبة", "Division")}><select required value={form.divisionId} onChange={e => set("divisionId", e.target.value)}><option value="">—</option>{divisions.map(x => <option key={x.id} value={x.id}>{pick("سنة", "Year")} {x.studyYear} · {pick("شعبة", "Division")} {x.number}</option>)}</select></FormField>}{form.audience === "Student" && <FormField label={pick("الطالب", "Student")}><select required value={form.studentUserId} onChange={e => set("studentUserId", e.target.value)}><option value="">—</option>{students.map(x => <option key={x.userId} value={x.userId}>{x.universityNumber} · {pick(x.nameArabic, x.nameEnglish)}</option>)}</select></FormField>}</ActionForm></Card><Card className="announcement-preview" title={pick("معاينة", "Preview")}><div className="preview-device"><div className="preview-appbar"><Megaphone size={17} /><span>{facultyName}</span></div><div className="preview-message"><div className="avatar accent"><Send size={16} /></div><div><strong>{form.titleArabic || form.titleEnglish || pick("عنوان الإعلان", "Announcement title")}</strong><p>{form.bodyArabic || form.bodyEnglish || pick("سيظهر نص الإعلان هنا أثناء الكتابة.", "Your announcement body will appear here as you type.")}</p><small>{pick("الآن", "Just now")}</small></div></div></div></Card></div></>;
}
